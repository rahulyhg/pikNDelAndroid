<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@mipmap/bg1"
    tools:context=".activity.intracity.DeliveryLocationActivity">
    <include
        layout="@layout/toolbar"/>
    <LinearLayout
        android:id="@+id/llLocation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/tvSingleLocation"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:padding="@dimen/dp_10"
            android:background="@drawable/rounded_bg_delivery"
            android:textColor="@color/dark_grey"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:text="@string/single_location"/>
        <TextView
            android:id="@+id/tvMultipleLocation"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:background="@drawable/rounded_bg_delivery"
            android:textColor="@color/dark_grey"
            android:padding="@dimen/dp_10"
            android:gravity="center"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:text="@string/multiple_location"/>\

    </LinearLayout>
    <FrameLayout
        android:id="@+id/flContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</LinearLayout>
